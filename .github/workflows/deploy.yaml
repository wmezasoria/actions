name: "workflow-name-WM"
on:
  push:
    branches: ["main"]
jobs:
  job-name-WM:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - run: echo "Deploying to workflow-WM" 

        